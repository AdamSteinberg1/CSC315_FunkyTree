#pragma once
//functions for creating a circle
#include <vector>
#include "Vec2.h"

class Circle : Polygon //a Circle is a Polygon with many sides, so it inherits
{
  Circle(int radius, Vec2 center)
};
